# Snake-Game
Snake game made using raylib C++

## How to Build and Run (Windows)

Make sure you have:
- raylib installed at `C:\raylib\raylib`
- w64devkit installed at `C:\raylib\w64devkit\bin`

Then, double-click `build_and_run.bat` or run it from the terminal:

```cmd
build_and_run.bat


